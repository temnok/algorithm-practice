package arrays;

public class Merge {
	// Merge sorted arr[l..m] and arr[m..r] into arr[l..r]
	public static void merge(int[] arr, int l, int m, int r) {
		MergeSolution.merge(arr, l, m, r); // TODO
	}
}
